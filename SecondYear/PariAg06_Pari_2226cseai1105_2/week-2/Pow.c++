double myPow(double x, int n) {
    // Write Your Code Here
  double result = pow(x, n);

    // Print the result with 6 digits after the decimal
    cout << fixed <<setprecision(6);
    return result;
}